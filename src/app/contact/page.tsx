import Navigation from "@/components/layout/Navigation";

export default function Contact() {
  return (
    <main style={{ padding: "var(--page-margin)" }}>
      <Navigation />
      <h1 style={{ marginTop: "20vh", fontSize: "var(--text-4xl)" }}>Contact</h1>
    </main>
  );
}
