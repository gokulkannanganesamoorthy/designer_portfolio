import Navigation from "@/components/layout/Navigation";

export default function Work() {
  return (
    <main style={{ padding: "var(--page-margin)" }}>
      <Navigation />
      <h1 style={{ marginTop: "20vh", fontSize: "var(--text-4xl)" }}>Work</h1>
    </main>
  );
}
